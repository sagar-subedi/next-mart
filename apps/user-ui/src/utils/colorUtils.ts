// Stub file for colorUtils - these functions are used by alternate components not currently in use

export const getColorNameFromHex = (hex: string): string => {
    return hex;
};

export const isLightColor = (hex: string): boolean => {
    return true;
};

export const formatColorName = (name: string): string => {
    return name;
};
